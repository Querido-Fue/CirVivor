/** 새 인게임 저장 파일에 사용하는 기본 최상위 데이터입니다. */
export const INGAME_DEFAULT_DATA = Object.freeze({
    current_level: 0,
    current_xp: 0,
    items: Object.freeze([])
});

/** 진행도 바이너리 저장 파일의 기본 바이트 길이입니다. */
export const PROGRESS_DEFAULT_BYTE_LENGTH = 128;
