/**
 * 적 관련 상수 모음
 */
export const ENEMY_CONSTANTS = Object.freeze({
    POOL_WARMUP_COUNT: 300,
    DEFAULT_STYLE: Object.freeze({
        FILL: '#ff6c6c',
        ALPHA: 1,
        ROTATION: 0
    })
});
